package body;

import javax.swing.*;
import java.awt.*;

public class wLabel extends JLabel {
    public wLabel(){
        setOpaque(true);
        setFocusable(true);
        setBackground(new Color(35,35,35));
        setForeground(Color.WHITE);
    }
}
